# Level 5 compact agent brief - product-owner draft

**Status:** Directional size-and-density reference only. This is not a validated
problem-bank fixture. The intentionally rough syntax records the sketch that
resolved the Level 5 direction and must be corrected before learner use.

```text
### 너는 저장소에서 작업하는 AI 코딩 에이전트다.

먼저 반드시 읽어라:
- AGENTS.md

목표:
Overwater 앱의 편지 탭을 "한눈에 들어오는 게시판"으로 리디자인한다. 목

중요 제약:
- 구현 전에 `git status --short`를 확인하고, 이 작업과 무관한 기존 변경은 건드리지 마라.
- 브랜드 자산 파일을 수정/생성/덮어쓰기 하지 마라:
  > apps/mobile/assets/icon.png

권장 접근:
 * B안을 구현해라. 
 //code
	Link : [goog][link]

구현 범위:
1. DB/API
 2. Mobile helpers/API
   3. Badge shelf
 * 1단계
 - 2단계
 + 3단계
 
테스트/검증:
** test를 추가한다**

완료 후:
- PROGRESS.md와 관련 docs/letters 문서를 필요한 만큼만 갱신한다. 이미 다른 사람이 수정한 변경을 덮어쓰지 마라.
- 최종 응답에는 변경 파일, 검증 결과, 남은 리스크만 간결히 정리한다.
```
